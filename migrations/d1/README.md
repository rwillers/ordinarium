# D1 migrations

Cloudflare D1 migrations will be generated and reviewed during migration Phase 4.
The baseline must come from a freshly migrated `main` SQLite database rather than
from the archived Python Worker staging implementation.
